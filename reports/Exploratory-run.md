# Exploratory run — 2026-09-03 00:01 UTC

Сначала выполнено 63 исследовательских HTTP-запроса, затем все 57 трассируемых тест-кейсов повторно запущены через Newman небольшими независимыми группами из-за высокой задержки публичного стенда. Итог Newman: **33 кейса пройдены, 24 провалены по ожидаемому контракту**. Проверялись JSON/XML, авторизация, CRUD, фильтры и негативные payload. Токены и служебные runtime-отчёты в репозиторий не включены.

Каждая группа генерировала уникальные данные. Cleanup удалял только точные bookingid, сохранённые из успешных POST-запросов этой же группы; поиск и массовое удаление по пользовательским полям не применялись.

## Подтверждённые отклонения

| Bug ID | Краткий результат |
| --- | --- |
| BUG-001 | Ошибочная аутентификация возвращает HTTP 200 |
| BUG-002 | Клиентские ошибки POST /booking приводят к HTTP 500 |
| BUG-003 | Обязательные строковые поля не валидируются |
| BUG-004 | Неверные типы и даты принимаются и повреждают данные |
| BUG-005 | XML depositpaid=false преобразуется в true |
| BUG-006 | Date-фильтры нарушают документированные условия и дают HTTP 500 на невалидном query |
| BUG-007 | Некорректная обработка Accept и Content-Type ответа |
| BUG-008 | ID с числовым префиксом и хвостом разрешается как валидный |
| BUG-009 | PUT/PATCH/DELETE отсутствующей брони возвращают 405 вместо 404 |

## Контрольные успешные результаты

- GET /ping → 201 Created.
- Валидный POST /auth → 200 и token.
- Валидный POST /booking → 200 и bookingid.
- GET созданной брони → 200 и совпадающие данные.
- PUT/PATCH с token → 200; изменения подтверждены повторным GET.
- PUT/PATCH/DELETE без авторизации и с неверным token → 403.
- DELETE с token → 201; последующий GET → 404.
- Невалидный JSON → 400.

Полные шаги, expected/actual и влияние приведены в `bug-reports/Bug-reports.xlsx`.
