{
  "name": "restful-booker-api-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Postman/Newman test assignment for Restful Booker API",
  "scripts": {
    "validate": "node scripts/validate-collection.mjs",
    "test": "npm run validate && newman run postman/Restful-Booker.postman_collection.json -e postman/Restful-Booker.postman_environment.json --reporters cli,json --reporter-json-export reports/newman-report.json",
    "test:report": "npm test -- --suppress-exit-code"
  },
  "devDependencies": {
    "newman": "6.2.1"
  }
}